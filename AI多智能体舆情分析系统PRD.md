# AI多智能体舆情分析系统PRD

## 一、文档概述

### 1.1 文档目的

明确「AI多智能体舆情分析」的核心需求、功能范围、交互逻辑和技术约束，为生成代码提供清晰指引，确保开发高效，最终实现一个“小而真”、可演示、有技术亮点的AI舆情项目。

### 1.2 产品定位

纯前端AI舆情分析Demo，基于React框架，集成多个AI智能体协同工作，调用现成大模型API，实现“关键词输入→多智能体协作分析→可视化结果+报告输出”的全流程。核心亮点为「多智能体自动协作」与「轻量化前端架构」，突出AI的落地能力与工程化设计。

### 1.3 核心目标

* 功能完整：实现5个AI智能体（采集、清洗、分析、洞察、报告）的真实协作，调用真实大模型API，非模拟数据。
* 前端可控：全程纯前端开发，无后端、无数据库、无复杂爬虫依赖，适配Vibecoding流畅开发节奏。
* 展示性强：界面简洁、科技感足，包含智能体运行动画、数据可视化、报告展示。
* 可扩展性：代码结构清晰，便于后续对接真实爬虫、扩展更多平台舆情采集、优化可视化效果。

### 1.4 适用场景

* 品牌部新品上市前的快速舆情摸底。
* 公关团队危机预警的轻量化工具。
* 产品经理竞品口碑分析辅助。

## 二、差异化创新点

* **架构创新**从“平台工具”升级为“多智能体自主团队”：摒弃传统大数据平台“规则配置+人工筛选+模块拼接”模式，采用多智能体原生架构，内置采集/清洗/分析/洞察/报告等专业Agent，自主分工、自主协作、自主决策，像一支AI分析团队在工作
* **流程创新**从“人操作机器”变成“机器全自动闭环”：告别传统“人工建规则、人工筛选、人工调报告、人工导出”模式，实现“一句话需求→全自动采集→清洗→分析→洞察→报告”，全程零人工干预，真正实现AI原生工作流
* **机制创新**“Agent论坛/辩论”机制，结论更可信：解决传统单一模型/规则输出的片面性问题，通过多个智能体交叉验证、辩论、互补，避免偏见，结论更深度、更严谨、可溯源
* **交互创新**从“配置面板”升级为“自然语言对话”：替代传统复杂后台、关键词组合、规则面板的高学习成本模式，采用聊天式交互，自然语言提需求，人人可用、轻量化上手
* **能力边界**从“舆情监测”升级为“舆情理解+预测”：突破传统监测、预警、可视化、报告的边界，实现趋势推演、风险预判、深层诉求挖掘、公私域数据融合，更接近“AI舆情顾问”

## 三、核心需求（重点参考）

### 3.1 核心功能需求

#### 3.1.1 关键词输入功能

* 提供输入框，支持用户输入舆情关键词（如品牌名、产品名、行业热点）。
* 输入框支持占位提示（例：请输入舆情关键词，如“新能源汽车”），支持回车触发分析。
* 输入校验：非空校验，关键词长度限制1-20字，避免无效输入。

#### 3.1.2 5个AI智能体协同功能（核心亮点）

智能体按“采集→清洗→分析→洞察→报告”顺序流水线执行，每个智能体执行完成后，反馈执行状态，最终输出完整结果。

| 智能体名称 | 核心职责                                           | 输入                             | 输出                                         | 依赖                        |
| ---------- | -------------------------------------------------- | -------------------------------- | -------------------------------------------- | --------------------------- |
| 采集Agent  | 获取舆情原始文本（前端安全模拟，可扩展真实爬虫）   | 用户输入的关键词                 | 原始舆情文本列表（含评论、讨论、吐槽等）     | 无（前端模拟，无需调用LLM） |
| 清洗Agent  | 去重、过滤广告、无效短句、冗余内容                 | 采集Agent输出的原始文本列表      | 清洗后的有效文本列表                         | 调用用户大模型API           |
| 分析Agent  | 情感分析（正/负/中性）、关键词提取、核心观点挖掘   | 清洗Agent输出的有效文本列表      | JSON格式结果（情感占比、关键词、观点总结）   | 调用用户大模型API           |
| 洞察Agent  | 分析舆情趋势、潜在风险、用户核心诉求、舆情爆发原因 | 分析Agent输出的分析结果          | JSON格式洞察结论（趋势、风险、诉求）         | 调用用户大模型API           |
| 报告Agent  | 整合所有智能体结果，生成正式、简洁的舆情分析报告   | 采集、清洗、分析、洞察的所有结果 | 文本格式舆情报告（含概况、情感、洞察、建议） | 调用用户大模型API           |

#### 3.1.3 智能体运行状态展示

* 展示5个智能体的执行顺序和当前状态（未运行/运行中/已完成/失败）。
* 每个智能体运行时，显示动态加载动画（如进度条、图标旋转），完成后显示“ 已完成”标识。
* 实时显示当前执行步骤（例：“清洗Agent 运行中...”），让演示者和观众清晰看到多智能体协作流程。

#### 3.1.4 数据可视化展示

* 核心图表：情感分布饼图（展示正面、负面、中性的占比）。
* 辅助展示：关键词标签云（提取分析Agent输出的关键词，按频率大小展示）。
* 图表要求：简洁、科技感，支持hover显示具体数值。

#### 3.1.5 舆情报告展示

* 展示报告Agent生成的完整舆情报告，格式清晰（分段、标题加粗）。
* 报告支持复制功能，便于演示时快速展示核心内容。
* 报告区域适配页面宽度，避免换行混乱，关键信息（如风险点、核心诉求）可轻微高亮。

#### 3.1.6 异常处理与降级方案

* **演示模式** ：当检测到API不可用或超时，自动提示是否切换至预设示例数据，确保大会演示不中断。
* **智能体执行中断** ：记录当前执行步骤，重新分析时可从中断步骤继续（可选，简化版可直接重新执行全流程）。
* **数据解析失败** ：显示提示（例：“数据解析异常，请重试”），避免页面崩溃。

### 3.2 交互需求

* 点击“开始分析”按钮后，禁用按钮（避免重复点击），同时启动智能体流程和状态展示。
* 智能体执行过程中，显示全局加载状态，禁止重复触发分析。
* 分析完成后，自动滚动到可视化图表区域，便于演示者讲解。
* 支持“重新分析”功能，点击后清空当前结果，重置所有智能体状态，可输入新关键词重新执行。
* 响应式适配：在大会演示的电脑屏幕（1920*1080）上正常显示，无布局错乱。
* 自然语言交互适配（补充）：预留自然语言输入交互入口，支持“一句话需求”式输入（如“分析新能源汽车近期舆情风险”），自动解析核心关键词并触发分析流程。

### 3.3 界面需求

* 整体风格：深色科技风（背景深色，文字浅色，重点内容用亮色强调），贴合AI大会氛围。
* 布局结构：顶部标题 → 关键词/自然语言输入区 → 智能体状态展示区 → 可视化图表区 → 报告展示区，自上而下，逻辑清晰。
* 组件样式：卡片式设计（带轻微阴影、圆角），智能体状态区用图标+文字区分，图表区和报告区独立卡片，视觉分层。
* 动画效果：智能体运行时的加载动画、步骤切换动画、结果展示的淡入动画，提升科技感，避免生硬。
* 交互适配（补充）：新增“自然语言对话框”样式选项，替代传统配置面板，支持聊天式输入交互，降低操作门槛。

## 四、技术约束与开发可行性

### 4.1 技术栈

* 框架：React（基于Vite构建），使用函数组件+Hook（useState、useEffect），代码简洁，适配Vibecoding开发节奏。
* 依赖包：axios（调用大模型API）、echarts（数据可视化），无需额外冗余依赖。
* 大模型API：封装独立服务（llmService.js），便于用户替换自己的API地址、密钥和模型参数。
* 代码结构：严格按照预设项目结构开发（agents/、services/、utils/、components/），每个文件职责单一，便于Copilot生成和后续维护。
* 扩展能力（补充）：
  - 新增agents/debateService.js：实现多智能体交叉验证/辩论逻辑，支持多Agent结论比对、互补修正。
  - 新增utils/nlpParser.js：自然语言需求解析工具，提取核心关键词、分析用户诉求类型（如风险分析、趋势预测）。
  - 新增utils/trendPredict.js：基于分析结果的舆情趋势推演、风险预判工具，支撑“AI舆情顾问”能力。

### 4.2 性能与稳健性

* 兼容性：适配主流浏览器（Chrome、Edge），无需兼容低版本浏览器（仅用于大会演示）。
* 性能要求：智能体执行流程流畅，无明显卡顿，API调用超时设置为10秒，超时提示用户重试或切换演示模式。
* 演示模式：内置示例数据（如关键词“新能源汽车”的模拟分析结果），当API不可用时一键降级，保证演示完整性。
* 多Agent协作性能（补充）：辩论/交叉验证流程需控制执行时长，单次全流程（含辩论）≤40秒，避免演示超时。

### 4.3 代码生成指引

- 智能体函数需为async/await异步函数，支持接收输入参数，返回对应格式结果（JSON或数组）。
- AgentFlow组件需接收loading和step参数，动态展示智能体运行状态和步骤，添加简单动画。
- ChartPanel组件接收analyzeResult参数，用echarts绘制情感饼图和关键词云，适配深色模式。
- 调度器agentOrchestrator.js需实现步骤回调（onStep），实时反馈当前执行状态，便于前端展示；新增多Agent辩论/交叉验证调度逻辑。
- 异常处理：在LLM调用、数据解析处添加try/catch，捕获异常并返回友好提示。
- 补充指引：
  - nlpParser.js需实现自然语言到结构化关键词的解析，支持提取“分析对象+分析维度”（如“新能源汽车+风险预判”）。
  - trendPredict.js需基于分析Agent的结果，生成趋势推演和风险预判结论，输出结构化JSON。
  - debateService.js需实现多智能体结论比对逻辑，当不同Agent结论偏差超过阈值时，触发二次验证并生成溯源说明。

## 五、典型用户与使用场景

| 用户角色     | 使用场景                                               | 价值点                                               |
| ------------ | ------------------------------------------------------ | ---------------------------------------------------- |
| 品牌部经理   | 新品发布后快速了解社交媒体口碑，无需等待专业舆情报告   | 分钟级获取情感分布、核心观点、潜在风险，支持快速决策 |
| 公关专员     | 日常监控品牌相关敏感词，及时发现负面舆情苗头           | 轻量化预警工具，降低人工巡检成本                     |
| 产品经理     | 分析竞品用户反馈，提取功能诉求与改进方向               | 辅助产品迭代方向判断，数据驱动                       |
| 大会演示观众 | 现场观看多智能体协作过程，理解AI如何自动化完成舆情分析 | 直观感受AI能力，展示公司技术实力                     |
| 非技术岗员工 | 通过自然语言提问（如“分析本月新品舆情风险”）获取报告 | 零学习成本，无需配置规则，快速获取专业分析结论       |

## 六、演示流程

1. 打开项目，显示简洁科技风界面，支持两种输入方式：①关键词输入（如“公司新品”）；②自然语言输入（如“分析公司新品近期舆情趋势和潜在风险”）。
2. 点击“开始分析”，智能体状态区依次显示“采集Agent运行中→清洗Agent运行中→...”，伴随加载动画；若开启辩论机制，可额外显示“多Agent交叉验证中...”状态。
3. 所有智能体执行完成后，自动展示情感饼图、关键词云，下方显示完整舆情报告（含趋势推演、风险预判、深层诉求等内容）；报告中可标注“结论溯源”说明（体现多Agent辩论结果）。
4. **（可选）演示模式切换** ：若现场网络波动导致API调用失败，点击“使用演示数据”，流程立即使用预设数据完成，展示降级后的完整报告，体现工程稳健性。
5. 演示者讲解：
   - 5个AI智能体的协同流程、每个智能体的作用、大模型API的调用逻辑；
   - 突出“前端+多智能体+降级方案”的技术亮点；
   - 演示自然语言交互、多Agent辩论、趋势预测等差异化能力。
6. 可点击“重新分析”，输入新关键词/自然语言需求，重复演示流程，展示Demo的稳定性和可复用性。

## 七、验收标准

* 功能验收：
  - 5个智能体可正常协同执行，调用真实大模型API，输出有效结果，无模拟数据（采集Agent除外）；演示模式可用。
  - 支持自然语言输入解析，可从一句话需求中提取核心关键词并触发分析。
  - 多Agent辩论/交叉验证机制生效，结论可溯源；支持趋势推演、风险预判输出。
* 交互验收：
  - 所有按钮、输入框可正常使用，智能体状态实时更新，异常情况有友好提示与降级选项。
  - 自然语言交互入口可用，解析结果准确，降低操作门槛。
* 界面验收：布局清晰、科技感强，动画流畅，适配大会演示屏幕，无布局错乱；自然语言对话界面样式符合设计要求。
* 性能验收：
  - API调用响应流畅，无明显卡顿，分析全流程（5个智能体+辩论机制）执行时间≤40秒；降级模式即时响应。
  - 多Agent辩论流程不影响整体演示流畅性。
* 代码验收：
  - 结构清晰，符合React开发规范，可直接运行，便于用户替换自己的大模型API。
  - 包含自然语言解析、多Agent辩论、趋势预测相关工具类代码，逻辑完整。
